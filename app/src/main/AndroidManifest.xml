<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookingproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Opcions_Activity"/>
        <activity
            android:name=".Menu_Principal_Activity"
            android:label="@string/title_activity_menu__principal_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Email_Activity" />
        <activity android:name=".Phone_Activity" />
        <activity android:name=".Google_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>